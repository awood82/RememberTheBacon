<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalwood.rememberthebacon" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".common.view.RememberTheBaconApplication"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".common.view.MasterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--activity android:name=".modules.list.ui.GroceryListActivity"
                  android:windowSoftInputMode="stateVisible"/>
        <activity android:name=".modules.details.ui.DetailsActivity"
            android:windowSoftInputMode="stateVisible"/-->
        <activity android:name=".common.view.TestFragmentActivity"/>
    </application>

</manifest>
